using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPM : MonoBehaviour
{
    public int numberOfItems, score;        //Ints represent WHOLE NUMBER
    public float health, weight, speed;     //Floats represemt DECIMAL NUMBER
    public string actualFavoriteStudent;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
